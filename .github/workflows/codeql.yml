name: CodeQL

on:
  push:
    branches: [main]
    paths:
      - "src/**/*.py"
      - "scripts/**/*.py"
      - "tests/**/*.py"
      - "pyproject.toml"
      - ".github/workflows/codeql.yml"
  schedule:
    - cron: "31 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: archiv-codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out repository without retained credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Initialize CodeQL
        uses: github/codeql-action/init@0963041ab44fef14b249c939dcda503ac8403e0c
        with:
          languages: python

      - name: Analyze Python
        uses: github/codeql-action/analyze@276e2ce25d959f0e04da2c6b73e26b3231395db1
        with:
          category: "/language:python"
