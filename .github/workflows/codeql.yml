name: CodeQL

on:
  push:
    branches: [main]
    paths:
      - "src/**/*.py"
      - "scripts/**/*.py"
      - "tests/**/*.py"
      - "pyproject.toml"
      - ".github/workflows/codeql.yml"
  schedule:
    - cron: "31 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

concurrency:
  group: archiv-codeql-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out repository without retained credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Initialize CodeQL
        uses: github/codeql-action/init@fa8392b7e54a5d74a53270a4aa7defa307aa415c
        with:
          languages: python

      - name: Analyze Python
        uses: github/codeql-action/analyze@fa8392b7e54a5d74a53270a4aa7defa307aa415c
        with:
          category: "/language:python"
