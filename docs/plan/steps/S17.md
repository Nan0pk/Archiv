# S17 — PPTX: speaker notes, tables, charts, grouped shapes, placeholder roles, hidden slides

**Phase C** (Recover the content being silently dropped) · depends on: S16

> **This step is a stub.** It carries the intent and the evidence behind it, but not a
> full capability declaration. Detail written months ahead of implementation is usually
> wrong, so expanding this stub — filling in the declaration block from
> [`docs/execution-contract.md`](../../execution-contract.md) and confirming the
> acceptance targets — is the first, separately-committed part of doing it.

## Why this step exists

Reproduced: a slide containing a title, a 3x2 table with six populated cells,
and a speaker note yielded **one segment** — the title. Roughly seven eighths of the
slide's information was dropped.

Two causes. `getattr(shape, "text", "")` returns `""` for a `GraphicFrame`, which is what
a table is, so the whole table is skipped silently. And speaker notes
(`slide.notes_slide`) are never read at all — frequently the densest text in a deck.
Grouped shapes are also not recursed, so anything grouped is invisible.

`normalize_odf.py:405` already handles `presentation:notes` for the OpenDocument
equivalent.

## Starting points

| Where | What |
|---|---|
| `src/archiv/ingestion/normalize_office.py:53-77` | `normalize_pptx` |
| `src/archiv/ingestion/normalize_odf.py:405` | notes handling, the reference |
| `python-pptx` | `shape.has_table`, `shape.has_chart`, `GroupShape.shapes`, `slide.notes_slide` |

## Draft acceptance

These targets are provisional. Confirm or replace them when expanding the stub, and
update [`queue.json`](../queue.json) to match.

```
tests/test_ooxml_recovery.py::test_slide_table_cells_are_extracted
tests/test_ooxml_recovery.py::test_speaker_notes_are_extracted
tests/test_ooxml_recovery.py::test_grouped_shapes_are_recursed
tests/test_ooxml_recovery.py::test_hidden_slides_are_flagged
```

## Before you start

- Read [`../DECISIONS.md`](../DECISIONS.md) — this step may already be constrained by a
  settled decision.
- Read [`../TRAPS.md`](../TRAPS.md).
- Fixtures are generated, never committed. Failure states get tests. Every new dependency
  is an optional extra.
