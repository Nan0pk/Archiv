# S10 — Rename image search to find-similar and delete the text embedder

**Phase B** (Stop the tool overclaiming) · depends on: S00

> **This step is a stub.** It carries the intent and the evidence behind it, but not a
> full capability declaration. Detail written months ahead of implementation is usually
> wrong, so expanding this stub — filling in the declaration block from
> [`docs/execution-contract.md`](../../execution-contract.md) and confirming the
> acceptance targets — is the first, separately-committed part of doing it.

## Why this step exists

`archiv images search` presents a ranked table with four-decimal scores, but the
text side of the embedder is a nineteen-entry colour lookup table with a hash-scatter
fallback for every other word: `seed = sum(ord(c) * 31**i)`, then
`vec[(seed + k*13) % 128] += 0.5`. Reproduced on `d9a9b8d`: the query
`"a photo of a person smiling"` ranked an invoice first at 0.4296, in a corpus containing
no people, with no match floor to stop it.

The image-to-image side is fine. Perceptual near-duplicate detection over colour and
edge features is a real, useful capability — the same scan filed in four folders is a
genuine user problem. Keep that, drop the text query, and rename so the name matches
what it does.

The recall@1 test asserts >= 0.70 over ten solid-colour images queried by their own
colour names — the same names hardcoded in the lookup table. It measures the table
against itself and must be replaced by something meaningful or removed.

## Starting points

| Where | What |
|---|---|
| `src/archiv/images/embedder.py:147-219` | `embed_text` — delete |
| `src/archiv/images/search.py:20-60` | drops the string-or-path mode guess; take `--image PATH` |
| `src/archiv/images/cli.py` | rename the subcommand |
| `tests/test_image_embeddings.py:195-231` | the self-referential recall test |

## Draft acceptance

These targets are provisional. Confirm or replace them when expanding the stub, and
update [`queue.json`](../queue.json) to match.

```
tests/test_image_similarity.py::test_no_text_query_surface_remains
tests/test_image_similarity.py::test_image_to_image_similarity_still_works
tests/test_image_similarity.py::test_results_below_the_match_floor_return_nothing
```

## Before you start

- Read [`../DECISIONS.md`](../DECISIONS.md) — this step may already be constrained by a
  settled decision.
- Read [`../TRAPS.md`](../TRAPS.md).
- Fixtures are generated, never committed. Failure states get tests. Every new dependency
  is an optional extra.
