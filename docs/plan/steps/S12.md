# S12 — Graph person becomes candidate_mention; remove hardcoded confidences

**Phase B** (Stop the tool overclaiming) · depends on: S11

> **This step is a stub.** It carries the intent and the evidence behind it, but not a
> full capability declaration. Detail written months ahead of implementation is usually
> wrong, so expanding this stub — filling in the declaration block from
> [`docs/execution-contract.md`](../../execution-contract.md) and confirming the
> acceptance targets — is the first, separately-committed part of doing it.

## Why this step exists

Person extraction is the regex `\b([A-Z][a-z]+ [A-Z][a-z]+)\b` filtered by a
hand-maintained English denylist. Reproduced on a five-line minutes fixture: three of ten
`person` nodes were people. The other seven were `The Working` (a truncation of "The
Working Group"), `Deep Learning`, `Machine Learning`, `Legal Counsel`, `Agenda Item`,
`New Delhi` — and `Terracotta Vase`, inherited from the face detector.

`archiv graph entity "New Delhi"` prints `Entity Profile: New Delhi (person)` with a
genuine citation and a "75.0%" co-occurrence confidence. The citation is real and
checkable — that part of the design works. What overclaims is the type and the number.

The denylist contains `new york`, `hong kong` and `los angeles` but not `new delhi`.
There is no version of that list that ends, which is why S27 replaces the approach rather
than extending the list.

## Starting points

| Where | What |
|---|---|
| `src/archiv/graph/builder.py:243-249` | the person regex |
| `src/archiv/graph/builder.py:28-80` | `_DISALLOWED_NAMES` / `_DISALLOWED_WORDS` |
| `src/archiv/graph/builder.py` | hardcoded 0.90 / 0.85 / 0.80 edge confidences |
| `src/archiv/graph/cli.py` | `entity` and `query` output rendering |
| `src/archiv/graph/contracts.py` | `NodeType` — add `candidate_mention` |

## Draft acceptance

These targets are provisional. Confirm or replace them when expanding the stub, and
update [`queue.json`](../queue.json) to match.

```
tests/test_entity_graph_precision.py::test_known_false_positives_are_not_typed_person
tests/test_entity_graph_precision.py::test_no_unmeasured_confidence_value_reaches_user_output
```

## Before you start

- Read [`../DECISIONS.md`](../DECISIONS.md) — this step may already be constrained by a
  settled decision.
- Read [`../TRAPS.md`](../TRAPS.md).
- Fixtures are generated, never committed. Failure states get tests. Every new dependency
  is an optional extra.
