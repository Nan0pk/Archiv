# S13 — Index staleness: refresh on add, report N of M, incremental image index

**Phase B** (Stop the tool overclaiming) · depends on: S00

> **This step is a stub.** It carries the intent and the evidence behind it, but not a
> full capability declaration. Detail written months ahead of implementation is usually
> wrong, so expanding this stub — filling in the declaration block from
> [`docs/execution-contract.md`](../../execution-contract.md) and confirming the
> acceptance targets — is the first, separately-committed part of doing it.

## Why this step exists

Reproduced: after adding three images, `archiv images status` reported
`Image index ready: 4 images indexed` while the object store held seven image objects.
Searches silently missed the three newest files. `archiv add` refreshes the FTS index
automatically and reports counts; it does not touch the image index or the entity graph,
and nothing reports the divergence.

`rebuild_image_index` is also full-rebuild only — it re-embeds every image every time —
while the text path gained an incremental `update_search_index` in PR #117. Mirror that.

This is a correctness bug, not a polish item: a confident "ready" over a stale index is
exactly the class of thing this project exists not to do.

## Starting points

| Where | What |
|---|---|
| `src/archiv/user_cli.py:400-450` | `add` — refreshes FTS only |
| `src/archiv/search/index.py:149` | `update_search_index` — the pattern to copy |
| `src/archiv/images/index.py:63-110` | `rebuild_image_index` — full rebuild only |
| `src/archiv/images/cli.py` | `status` — must report indexed against total |
| `src/archiv/graph/builder.py:101` | `rebuild_graph` — same problem |

## Draft acceptance

These targets are provisional. Confirm or replace them when expanding the stub, and
update [`queue.json`](../queue.json) to match.

```
tests/test_index_freshness.py::test_add_refreshes_the_image_index_and_graph
tests/test_index_freshness.py::test_status_reports_indexed_count_against_object_count
tests/test_index_freshness.py::test_image_index_update_is_incremental
```

## Before you start

- Read [`../DECISIONS.md`](../DECISIONS.md) — this step may already be constrained by a
  settled decision.
- Read [`../TRAPS.md`](../TRAPS.md).
- Fixtures are generated, never committed. Failure states get tests. Every new dependency
  is an optional extra.
